export default function QuizPage() {
  return (
    <section className="panel">
      <h2>Quiz workspace (placeholder)</h2>
      <p>
        Quiz generation, attempts, and scoring feedback will be delivered in
        Phase 3.
      </p>
    </section>
  );
}
